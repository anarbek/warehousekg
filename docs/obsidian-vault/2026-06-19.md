# 2026-06-19 — Vehicle Management Module Implementation

## Completed

### Domain Layer
- ✅ 6 new enums: `VehicleStatus`, `VehicleOwnershipType`, `MaintenanceType`, `InspectionResult`, `FuelType`, `VehicleType`
- ✅ 7 new entities in `Domain/Entities/Vehicles/`: Vehicle, VehicleType, VehicleDriverAssignment, VehicleMaintenanceRecord, VehicleInsuranceRecord, VehicleInspectionRecord, VehicleFuelRecord
- ✅ 6 new authorization resources added to `Identity/Resources.cs`

### Infrastructure
- ✅ 7 EF configuration files in `Persistence/Configurations/Vehicles/`
- ✅ Snake-case table naming, enum string conversions, cascade/restrict delete behaviors
- ✅ 7 DbSet properties added to `IApplicationDbContext` and `WarehouseKgDbContext`
- ✅ Migration `20260619112152_AddVehicleManagement` created and applied

### Application Layer (CQRS)
- ✅ Vehicle Types: Create, Update, Delete commands + GetById, GetList queries
- ✅ Vehicles: Create, Update, Delete commands + GetById, GetList, GetDetail queries with DTOs
- ✅ Driver Assignments: Create, Update, Delete commands + queries by vehicle/employee
- ✅ Maintenance: Create, Update, Delete commands + queries (per-vehicle + fleet-wide)
- ✅ Insurance: Create, Update, Delete commands + queries (per-vehicle + fleet-wide)
- ✅ Inspections: Create, Update, Delete commands + queries (per-vehicle + fleet-wide)
- ✅ Auto-update: maintenance→vehicle next-due, insurance→vehicle expiry, inspection→vehicle expiry
- ✅ DateTime Kind fix: `DateTime.SpecifyKind(..., DateTimeKind.Utc)` on all DateTime assignments (PostgreSQL `timestamptz` requires UTC)

### API Controllers
- ✅ `VehicleTypesController` — `/api/v1/vehicle-types`
- ✅ `VehiclesController` — `/api/v1/vehicles` + detail endpoint
- ✅ `DriverAssignmentsController` — `/api/v1/vehicles/{id}/assignments`
- ✅ `MaintenanceController` — `/api/v1/vehicles/{id}/maintenance`
- ✅ `InsuranceController` — `/api/v1/vehicles/{id}/insurance`
- ✅ `InspectionsController` — `/api/v1/vehicles/{id}/inspections`
- ✅ `FleetRecordsController` — `/api/v1/maintenance`, `/api/v1/insurance`, `/api/v1/inspections`

### Auth & Seeding
- ✅ IdentitySeeder: Dispatcher gets write+delete on all 6 vehicle resources
- ✅ TenantPermission rows seeded for Dispatcher role

### Frontend — Models & Services
- ✅ `vehicles.model.ts`: All interfaces (VehicleType, Vehicle, VehicleDetail, DriverAssignment, MaintenanceRecord, InsuranceRecord, InspectionRecord) + request types
- ✅ `vehicles.service.ts`: Full CRUD for all 6 sub-features + fleet-wide getAll queries

### Frontend — Components (11 routes)
- ✅ `vehicle-type-list` / `vehicle-type-form` — grid + create/edit form
- ✅ `vehicle-list` — grid with status badges, driver, insurance column
- ✅ `vehicle-form` — 24-field form with Russian labels, 2-column layout, dropdowns
- ✅ `vehicle-detail` — tabbed view (Info, Maintenance, Insurance, Inspections, Drivers) with popup add/edit/delete for each tab
- ✅ `maintenance-list` — fleet-wide grid with add popup (vehicle dropdown + form)
- ✅ `insurance-list` — fleet-wide grid with add popup (vehicle dropdown + form)
- ✅ `inspection-list` — fleet-wide grid with add popup (vehicle dropdown + form)

### Frontend — Navigation
- ✅ Sidebar "Автопарк" group with 5 items: Транспорт, Типы транспорта, ТО, Страховка, Техосмотр
- ✅ Admin bypass in sidenav filter

### Bugs Fixed
- ✅ Angular route ordering: static paths (`types`, `maintenance`, etc.) before `:id`
- ✅ Date timezone shift: `isoToDateStr()` extracts `YYYY-MM-DD` from ISO via regex, date boxes use `dateSerializationFormat`
- ✅ Popup toolbar rendering: replaced `dxo-toolbar`/`dxi-item` with `<div class="popup-actions">` + `<dx-button>`
- ✅ Driver dropdown empty: moved `formItems.set()` after data loads
- ✅ Driver duplicate on save: skip assignment if employee unchanged
- ✅ Driver assignment popup: added date range (Назначен с / По)
- ✅ Edit form driver removal: vehicle form no longer includes driver dropdown
- ✅ Fleet popup buttons: switched from toolbar widgets to plain buttons

### Documentation
- ✅ Created `docs/obsidian-vault/10-Vehicle-Management.md` — full module reference

## Pending
- ⬜ Fleet grids: edit/delete buttons (need `vehicleId` added to fleet DTOs)
- ⬜ Fuel logs CRUD (entity exists, not implemented)
- ⬜ Vehicle list search/filter improvements
